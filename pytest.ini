[pytest]
# The library lives in the itasorl/ package at the repo root, so put the root on
# sys.path for `import itasorl.world` etc. from tests/ without an editable install.
pythonpath = .
testpaths = tests
